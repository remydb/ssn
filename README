README Traitor Trace VFS module
===================================================

This directory contains the VFS module.
To use just execute the following commands:

./configure
make
make install

The module will be compiled and installed in the samba direcotry containing all VFS libraries.

Example smb.conf:
[share1]
path = /var/samba/share1
vfs objects = traitor_trace
traitor_trace:script = /var/samba/process.py
browseable=yes
writeable=yes
